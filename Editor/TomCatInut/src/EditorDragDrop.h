#pragma once

namespace TomCat {

	// Editor-only entity identity payload. Asset payloads remain defined by the
	// runtime Asset API; this one is shared only by Hierarchy and Project panels.
	inline constexpr const char* SceneEntityDragDropPayloadID = "SCENE_ENTITY_UUID";

}
